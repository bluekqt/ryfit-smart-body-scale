# 云悦(RyFit)智能体质分析仪蓝牙通信协议

## 设备信息
- **体脂秤名称**：云悦(RyFit)智能体质分析仪
- **型号**：RyFit-A01
- **蓝牙设备名称**：ChronoCloud
- **MAC 前缀**：`D0:39:72` (Texas Instruments)
- **蓝牙类型**：低功耗蓝牙 4.0 (BLE)
- **服务 UUID**：`0000ffe0-0000-1000-8000-00805f9b34fb`
- **通信特征值**
  - **通知 / 写入**：`0000ffe1-0000-1000-8000-00805f9b34fb`
  - **配置写入（可选，未使用）**：`0000ffe3-0000-1000-8000-00805f9b34fb`

> 实际通信完全通过 `0xffe1` 进行。
> 
> **术语说明**：本文档中的 **"用户编号"** 指秤上显示的 **P1、P2 … P8**，对应 8 个可独立存储用户数据的位置。游客模式使用编号 `09`，秤上会显示 P9 标记和性别，但秤不会本地保存。

---

## 数据包概述

秤通过 **Handle Value Notification** 主动发送多种类型的有效数据包。以下按功能分类。

### 1. 实时重量变化（D2 包）
- **格式**：`D2` + 占位(1) + 设备标志(1) + 体重(2，ASCII hex)
- **示例**：
```
收到: D2 01 02 9A
解析: 体重 = "029A" = 666 → 66.6 kg
```
- **说明**：测量过程中持续发送,直到数据稳定,秤进行计算数据后发回第一数据包,不计入历史存储。**无需回复确认**。

### 2. 第一数据包（体重、体脂率、水含量）
- **识别标志**：`data[2]` 为奇数。实时测量时固定为 `0x01`；历史记录中依次为 01, 03, 05…（每条记录的第一包）。
- **字节序**：体重/体脂/水分均为 **ASCII 编码的十六进制文本**

| 偏移 | 长度 | 说明 | 解析方法 |
|------|------|------|----------|
| 0    | 1    | `D0` 或 `D1` | 实时为 D0，历史为 D1 |
| 1    | 1    | 总数据包数 | 该用户编号下所有历史记录的数据包总数 |
| 2    | 1    | 包序号（奇数） | 同一用户编号历史记录的唯一标识 |
| 3    | 1    | 用户编号 | `01`–`08`（秤上显示 P1–P8），`09` 为游客 |
| 4‑7  | 4    | 时间戳（小端序） | Unix 时间 (uint32) |
| 8‑9  | 2    | 体重 | ASCII hex，单位 0.1 kg |
| 10‑11| 2    | 体脂率 | ASCII hex，单位 0.1% |
| 12‑13| 2    | 水含量 | ASCII hex，单位 0.1% |

**解析示例**：
```
收到: D1 06 01 03 CF AE F4 69 02 9D 00 FD 01 FC

字段解析:
  包类型:  D1 (0xD1, 历史数据)
  总数据包数: 06 (0x06 → 该用户编号下共有 6 个数据包)
  序号:   01 (0x01, 记录1 第一包)
  用户编号: 03 (0x03, 对应秤上 P3)
  时间戳: CF AE F4 69 → 0x69F4AECF (小端序) → 2026-05-01 21:47:43
  体重:   02 9D → 0x029D = 669 → 66.9 kg
  体脂率: 00 FD → 0x00FD = 253 → 25.3%
  水含量: 01 FC → 0x01FC = 508 → 50.8%
```

> 奇偶序号成对（如 01+02）构成一条完整的测量记录。

### 3. 第二数据包（肌肉、骨量、代谢、皮下脂肪、内脏脂肪、身体年龄）
- **识别标志**：`data[2]` 为偶数（02,04,06…）
- **触发方式**：收到第一数据包并回复 `FA` + 该包前 3 字节确认后，秤**自动推送**第二数据包，无需主动请求。
- **字节序**：大端 (Big Endian)
- **注意**：此数据包**不包含时间戳**，时间与同序号的上一奇数包对应。

| 偏移 | 长度 | 说明 | 解析方法 |
|------|------|------|----------|
| 0    | 1    | `D0` 或 `D1` | |
| 1    | 1    | 总数据包数 | 该用户编号下所有历史记录的数据包总数 |
| 2    | 1    | 包序号（偶数） | 与上一奇数序号配对 |
| 3    | 1    | 用户编号 | 秤上显示 P1–P8 |
| 4‑5  | 2    | 肌肉比例 | uint16 BE，单位 0.1% |
| 6‑7  | 2    | 骨量 | uint16 BE，单位 0.1% |
| 8‑9  | 2    | 基础代谢 (亚洲) | uint16 BE，单位 kcal |
| 10‑11| 2    | 皮下脂肪 | uint16 BE，单位 0.1% |
| 12   | 1    | 内脏脂肪 | uint8，单位 级 |
| 13   | 1    | 身体年龄 | uint8，单位 岁 |

**解析示例**：
```
收到: D1 06 02 03 01 BE 00 27 05 49 00 E5 06 26

字段解析:
  包类型:   D1 (0xD1, 历史数据)
  总数据包数: 06 (0x06 → 该用户编号下共有 6 个数据包)
  序号:     02 (0x02, 记录1 第二包，与 0x01 配对)
  用户编号: 03 (0x03, 对应秤上 P3)
  肌肉比例: 01 BE → 0x01BE = 446 → 44.6%
  骨量:     00 27 → 0x0027 = 39 → 3.9%
  基础代谢: 05 49 → 0x0549 = 1353 kcal
  皮下脂肪: 00 E5 → 0x00E5 = 229 → 22.9%
  内脏脂肪: 06 → 0x06 = 6 级
  身体年龄: 26 → 0x26 = 38 岁
```

> 奇偶序号成对（如 01+02）构成一条完整的测量记录。

### 4. 用户编号信息包（B5 包）
- **触发指令**：`A5`
- **结构**：

| 偏移  | 长度  | 说明                    |
| --- | --- | --------------------- |
| 0   | 1   | `B5`                  |
| 1   | 1   | 当前已注册用户总数             |
| 2   | 1   | 序号                    |
| 3   | 1   | **用户编号** (与秤上显示 Px 对应) |
| 4   | 1   | 身高编码                  |
| 5   | 1   | 年龄                    |
| 6   | 1   | 性别 (`01`男, `00`女)     |

**解析示例**：
```
收到: B5 02 01 01 A6 28 01

字段解析:
  用户总数: 02 (0x02)
  序号:     01 (0x01)
  用户编号: 01 (0x01, 对应秤上 P1)
  身高编码: A6 → 0xA6 = 166 → 183 cm
  年龄:     28 → 0x28 = 40 岁
  性别:     01 → 0x01 = 男
```

---

## 控制命令（通过 `0xffe1` 写入）

### 1. 时间同步
- **命令**：`FA F8` + 4 字节 Unix 时间戳（小端序）
- **响应**：`FB F8`
- **说明**：每次连接后必须首先发送，用于校准秤内时钟。

**示例**：
```
发送: FA F8 5E 9F F4 69
收到: FB F8
```

### 2. 查询已注册用户情况
- **命令**：`A5`
- **响应**：`FB A5 00 [已注册用户数] 08` + 若干 `B5` 包
- **说明**：每个已注册用户编号会收到一个 B5 包，需回复 `FA` + 该 B5 包前 3 字节确认，秤才会发送下一条 B5。若只需确认单个编号是否占用，可直接使用 `C1` 指令更高效。

**示例**：
```
发送: A5
收到: FB A5 00 02 08          ← 2 个已注册用户
收到: B5 02 01 01 A6 28 01    ← 用户编号01 (P1): 男, 183cm, 40岁
发送: FA B5 02 01             ← 确认收到 B5 序号 01 包
收到: B5 02 02 03 8C 21 01    ← 用户编号03 (P3): 男, 170cm, 33岁
发送: FA B5 02 02             ← 确认收到 B5 序号 02 包
```

### 3. 激活用户编号（准备注册）
- **命令**：`A1` + 用户编号(1) + 身高编码(1) + 年龄(1) + 性别(1)
- **响应**：`FB A1 00` 成功，`01` 失败，`03` 格式错误
- **说明**：必须携带完整参数。成功后还需发送 `C0` 最终写入。

**示例**：
```
发送: A1 03 8C 21 01         ← 用户编号03 (P3),170cm,33岁,男
收到: FB A1 00               ← 激活成功
```

### 4. 创建/切换用户
- **命令**：`C0` + 用户编号(1) + 身高编码(1) + 年龄(1) + 性别(1)
- **响应**：`FB C0 00` 成功，`FB C0 01` 失败
- **说明**：
  - 在 `A1` 激活后发送，完成最终注册。
  - 注册成功后在秤未连接蓝牙时,秤会根据体重显示 Px(用户编号)+性别。例如 "P3 男"。
  - **对已注册的用户编号**，可直接发送 `C0`来切换到该用户，解决秤靠体重区分用户导致的同体重冲突问题。建议在每次测量前发送一次 `C0` 以明确当前用户。

**示例**：
```
发送: C0 03 8C 21 01         ← 用户编号03 (P3),170cm,33岁,男
收到: FB C0 00               ← 创建/切换成功,秤上显示对应PX+性别
```

### 5. 删除用户编号及其历史数据
- **命令**：`A2` + 用户编号(1)
- **响应**：`FB A2 00` 成功，`FB A2 01` 失败
- **说明**：`B5` 包会提供所有已注册的用户编号。此命令会**删除该用户编号及其全部历史记录**，秤上对应的 Px 显示将消失。若只想清除历史而保留用户注册信息，请使用 `C2` 指令。

**示例**：
```
发送: A2 01                  ← 删除用户编号为 01 (P1) 的记录
收到: FB A2 00               ← 删除成功
```

### 6. 查询用户历史数据
- **命令**：`C1` + 用户编号(01-08)
- **响应**：
  - `FB C1 00 00`：用户编号已激活但无测量数据。**需要用 `A2` 删除该用户编号后重新注册**，否则无法开始正常使用。
  - `FB C1 00 01`：用户编号空闲（未激活），可直接使用 `A1 + C0` 注册。
  - `FB C1 [总数据包数] 00` + 若干 `D1` 包：有历史数据。随后秤会依次发送每条历史记录的完整数据包对：
    - 奇数序号（01,03,05…）为第一数据包（体重/体脂/水分）
    - 偶数序号（02,04,06…）为第二数据包（肌肉/骨量/代谢等）
  - 每个不同序号的 `D1` 包都需要回复 `FA` + 该包前 3 字节确认；若未确认，秤会**重复发回约 3 次**，之后若仍未确认，秤将进入休眠 / 停止重发。

**示例**：
```
发送: C1 03                                     ← 查询用户编号 03 (P3)
收到: FB C1 0A 00                               ← 总数据包数 0x0A (10)
收到: D1 0A 01 03 CF AE F4 69 02 9D 00 FD 01 FC ← 记录1 第一包
发送: FA D1 0A 01                               ← 确认第一包
收到: D1 0A 02 03 01 BE 00 27 05 49 00 E5 06 26 ← 记录1 第二包
发送: FA D1 0A 02                               ← 确认第二包
收到: D1 0A 03 03 EE B1 F4 69 02 9C 00 F8 01 FF ← 记录2 第一包
发送: FA D1 0A 03                               ← 确认第一包
收到: D1 0A 04 03 01 C2 00 28 05 4F 00 E1 06 26 ← 记录2 第二包
发送: FA D1 0A 04                               ← 确认第二包
...（后续记录依此类推，直到 5 秒无新包）
```

### 7. 清空用户历史记录（保留用户注册信息）
- **命令**：`C2` + 用户编号(1)
- **响应**：`FB C2 00` 成功，`FB C2 01` 失败
- **说明**：该命令会**立即删除指定用户编号的全部历史测量记录**，但不影响该编号的注册状态。此后再次查询 C1 将返回 `FB C1 00 00`（已激活但无数据）。常用于同步完成后清理秤上存储，以加速下次同步。

**示例**：
```
发送: C2 01                  ← 清除用户编号 01 (P1) 的全部历史记录
收到: FB C2 00               ← 清空成功
```

### 8. 获取设备码
- **命令**：`A1 5A`（两字节）
- **响应**：`FB A1 05` + 3 字节设备码
- **说明**：设备码可由 `A1 A5` 命令重新写入，并非永久固定。若秤内设备码为 `FFFFFF`，表示尚未分配有效设备码。

**示例**：
```
发送: A1 5A
收到: FB A1 05 72 A1 C9      ← 设备码 72A1C9
```

### 9. 写入设备码
- **命令**：`A1 A5` + 3 字节设备码
- **响应**：`FB A1 05`
- **说明**：可多次写入，后写的设备码会覆盖之前的值，用于设备绑定或更换绑定。

**示例**：
```
发送: A1 A5 72 A1 C9         ← 写入设备码 72A1C9
收到: FB A1 05               ← 写入成功
```

### 10. 确认数据包（通用）
- **命令**：`FA` + 所收到数据包的前 3 个字节

**针对不同数据包的确认规则**：

| 数据包类型 | 确认后行为 |
|------------|------------|
| **D0 实时第一包** (`D0 02 01 XX`) | 秤自动推送当前测量的第二包，后续不会再推送其他历史包。 |
| **D1 历史第一包** (`D1 [总包数] [奇数序号] XX`) | 秤自动推送该记录的第二包。确认第二包后，秤继续推送下一条记录的第一包。 |
| **D0/D1 第二包** (`D0/D1 [总包数] [偶数序号] XX`) | 秤停止发送该记录的数据；若还有历史记录，继续推送下一条的第一包。 |
| **B5 包** (`B5 [用户数] [序号] [编号] ...`) | 秤继续发送下一条 B5 包（若还有）；若已是最后一条，停止发送。 |
| **其他** (D2/F8/FB…) | 无需确认 |

> 注意：对第一数据包确认后，秤会自动推送对应的第二数据包；对第二数据包确认后，秤推送下一条记录的第一包；所有数据包确认后即停止重复。

**示例**：
```
收到: D0 02 01 03 ...
确认: FA D0 02 01

收到: D1 0A 01 03 ...
确认: FA D1 0A 01

收到: D1 0A 02 03 ...
确认: FA D1 0A 02

收到: B5 02 01 01 A6 28 01
确认: FA B5 02 01
```

---

## 用户配置字节编码

| 字段   | 长度  | 编码规则                    | 示例              |
| ---- | --- | ----------------------- | --------------- |
| 用户编号 | 1   | `01`‑`08`（秤上显示 P1–P8），`09` 为游客  | `03` (P3)       |
| 身高编码 | 1   | `编码 = 2 × 身高(cm) - 200` | 170 cm → `0x8C` |
| 年龄   | 1   | 年龄的十六进制值                | 33 → `0x21`     |
| 性别   | 1   | `0x00` 女，`0x01` 男       |                 |

**配置命令示例**：
```
男性, 170 cm, 33 岁 
→ 注册: A1 03 8C 21 01 + C0 03 8C 21 01
→ 秤上显示: P3 男

女性, 160 cm, 40 岁
→ 注册: A1 03 78 28 00 + C0 03 78 28 00
→ 秤上显示: P3 女
```

---

## 完整流程示例

### 注册新用户

**注册内容**：用户编号 03 (P3)、身高 170 cm、年龄 33 岁、性别 男

**流程**：

```
1. 连接 → 收到: F8
2. 发送: FA F8 + 当前时间戳      → 收到: FB F8
3. 发送: C1 03                   → 收到: FB C1 00 01（编号空闲）
4. 发送: A1 03 8C 21 01          → 收到: FB A1 00
5. 发送: C0 03 8C 21 01          → 收到: FB C0 00（秤显示 P3 男）
6. 上秤 → D2 实时重量 → 稳定后 D0 第一数据包
7. 发送: FA D0 02 01             → 秤自动推送 D0 第二数据包
8. 发送: FA D0 02 02             → 测量完成，数据自动保存
```

---

### 切换已注册用户（避免同体重冲突）

**切换至**：用户编号 03 (P3)、身高 170 cm、年龄 33 岁、性别 男
**秤上同步显示**：P3 男

**流程**：

```
1. 连接 → 收到: F8
2. 发送: FA F8 + 当前时间戳      → 收到: FB F8
3. 发送: C0 03 8C 21 01         → 收到: FB C0 00（秤显示 P3 男）
4. 可上秤测量
```

> 切换已注册用户只需发送 `C0`，无需再次 `A1` 激活。

---

### 查询历史数据

**查询内容**：用户编号 03 (P3) 的所有历史测量记录

**流程**：

```
1. 连接 → 收到: F8
2. 发送: FA F8 + 当前时间戳      → 收到: FB F8
3. 发送: C1 03                   → 收到: FB C1 0A 00（总数据包数 10）
4. 收到: D1 0A 01 03 ...         → 发送: FA D1 0A 01
5. 收到: D1 0A 02 03 ...         → 发送: FA D1 0A 02
6. 收到: D1 0A 03 03 ...         → 发送: FA D1 0A 03
7. 收到: D1 0A 04 03 ...         → 发送: FA D1 0A 04
   ...循环确认所有记录对（01+02=记录1, 03+04=记录2...）
8. 超时 5 秒无新包 → 查询结束
```

> 每条测量记录由奇数序号包（体重/体脂/水分）和偶数序号包（肌肉/骨量/代谢等）成对组成。
> 每个未确认的包会重复发送约 3 次后休眠，必须及时确认。

---

## 状态码速查

| 状态码 | 含义 |
|--------|------|
| `00`   | 成功 / 无错误 |
| `01`   | 失败 / 用户编号已占用 / 格式错（视上下文） |
| `03`   | 命令格式错误（参数数量或长度不正确） |

---

## 开发建议

- **数据解析**：第一包使用 ASCII hex 文本解析体重/体脂/水分；第二包使用大端二进制解析详细体成分。切勿混淆两种解析方式。
- **确认机制**：D0/D1 数据包、B5 用户信息包需要回复 `FA + 前3字节` 确认，否则秤会重复发送。D2 实时重量、F8 上电通知、FB 状态响应无需确认。
- **重发与休眠**：未确认的数据包约重复 3 次后秤会休眠，不再发送，因此必须在收到包后及时确认。
- **时间同步**：每次连接必须先发送 `FA F8` + 当前时间戳，否则后续命令可能不生效。
- **优化同步**：同步历史数据后，可使用 `C2` 命令一键清空该槽位的全部历史记录，大幅减少下次同步的数据量。注意 `C2` 仅清除历史，不会删除用户注册信息；如需彻底删除用户，请使用 `A2`。
- **用户设置**：新用户编号必须 `A1 → C0` 两步完成；已有用户的编号可直接 `C0` 切换，无需再次 A1。
- **用户编号冲突**：称重前发送 `C0` 指定当前用户编号，可解决同体重无法自动识别用户的问题。
- **空数据用户（FB C1 00 00）**：若查询返回 `FB C1 00 00`，表示用户编号已激活但从未测量，需用 `A2` 删除后重新 `A1 + C0` 注册。
- **设备码**：可通过 `A1 A5` 多次写入覆盖，无需永久绑定。若设备码为 `FFFFFF`，表示未写入过，历史数据可能无法保存。
- **数据包顺序**：C1 历史查询时，所有记录按内部序号顺序发送。每条记录的序号成对连续（如 01 和 02），可根据序号配对。
- **超时等待**：历史查询结束后建议等待 5 秒无新数据再断开连接，确保所有记录均已接收并确认。